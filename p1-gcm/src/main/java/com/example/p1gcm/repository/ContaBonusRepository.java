package com.example.p1gcm.repository;

import com.example.p1gcm.model.ContaBonus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContaBonusRepository extends JpaRepository<ContaBonus, String> {
}
